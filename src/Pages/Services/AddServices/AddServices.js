import React from 'react';
import Title from '../../../Hooks/Title';

const AddServices = () => {
    Title("Add Services");
    return (
        <div>
            
        </div>
    );
};

export default AddServices;